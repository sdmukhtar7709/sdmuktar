{
  "name": "sdmuktar",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio website deployment config for Render",
  "scripts": {
    "start": "serve . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
